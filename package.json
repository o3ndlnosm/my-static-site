{
    "name": "my-static-site",
    "version": "1.0.0",
    "scripts": {
      "build": "echo 'Building the project...' && mkdir -p build && cp -r public/* build/"
    },
    "dependencies": {},
    "devDependencies": {}
  }
  